## 复制为 .env 后按需修改（.env 默认不会提交）
## cp .env.example .env

# 应用名称（桌面显示名）
APP_NAME=怂团

# 包名 / App ID（注意：修改后相当于另一个应用）
APP_ID=com.h5app.app

# Web 静态资源目录（需包含 index.html）
WEB_DIR=dist

# WebView 请求代理（build-app.sh 注入代理脚本时使用）
# 为空则不注入代理脚本；需要时在本地 .env 里填写真实地址
PROXY_BASE_URL=

