## 复制为 .env 后按需修改（.env 默认不会提交）
## cp .env.example .env

# 应用名称（桌面显示名）
APP_NAME=怂团

# 包名 / App ID（注意：修改后相当于另一个应用）
APP_ID=com.h5app.app

# Web 静态资源目录（需包含 index.html）
WEB_DIR=dist

# 本机代理端口（0 表示自动分配随机可用端口）
# 若指定固定端口（如 5173），被占用时会自动回退到随机端口
LOCAL_PROXY_PORT=0

